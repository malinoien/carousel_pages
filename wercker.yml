box: debian
build:
  steps:
  - arjen/hugo-build:
      version: "0.17"
      theme: heather-hugo
      flags: --buildDrafts=true

deploy:
  steps:
  - install-packages:
      packages: git ssh-client
  - lukevivier/gh-pages:
      token: $GIT_TOKEN
      basedir: public
      repo: malinoien/malinoien.github.io
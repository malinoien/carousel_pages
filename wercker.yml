box: debian
build:
  steps:
  - arjen/hugo-build:
      version: "0.17"
      theme: heather-hugo
      flags: --buildDrafts=true
      config: config.toml

deploy:
  steps:
  - install-packages:
      packages: git ssh-client
  - lukevivier/gh-pages:
      token: $GIT_TOKEN
      basedir: public
      repo: malinoien/malinoien.github.io